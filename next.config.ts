import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['picsum.photos', 'i.ytimg.com', 'img.youtube.com','images.unsplash.com','cautious-starfish.static.domains'],
  },
};

export default nextConfig;
